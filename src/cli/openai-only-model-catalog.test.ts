// NOTE: The "OpenAI-only model catalog" tests were deleted on 2026-04-17 when
// API-key providers (openai direct) were removed. Subscription-only routing
// via github-copilot / opencode supersedes this path. The catalog branch
// still exists in `openai-only-model-catalog.ts` but is no longer reachable
// because the `openai` provider is denied by the allowlist.
//
// If reinstated in the future, cover the new subscription routing here.

export {}
